---
phase: 05-toolbar-integration
plan: 01
type: execute
---

<objective>
Move existing controls to native macOS toolbar with Finder-like behavior.

Purpose: Replace inline buttons with a proper macOS toolbar for polished native appearance.
Output: Toolbar with folder picker, path display, and caption action buttons.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Codebase constraints:**
- macOS 15.5+ deployment target
- Pure SwiftUI + AppKit (no external packages)
- MVVM with @MainActor ViewModel
- FocusedValue pattern already established for menu commands

**Current controls to relocate:**
1. "Escolher Pasta" button - sidebar header (ContentView:16)
2. Path display text - sidebar header (ContentView:24-31)
3. "Salvar" button - DetailView (line 232)
4. "Recarregar Caption" button - DetailView (line 192)

**SwiftUI toolbar approach:**
- `.toolbar` modifier on NavigationSplitView
- `ToolbarItem(placement:)` for positioning
- Leading: folder navigation
- Trailing: caption actions (save, reload)

**Prior decisions affecting this phase:**
- FocusedValue pattern established in Phase 2 for menu commands (reuse for toolbar access to VM)
- Save button tied to selectedIsDirty state (preserve this behavior)

@lora-dataset/lora-dataset/ContentView.swift
@lora-dataset/lora-dataset/lora_datasetApp.swift
@lora-dataset/lora-dataset/DatasetViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toolbar with folder navigation controls</name>
  <files>lora-dataset/lora-dataset/ContentView.swift</files>
  <action>
Add `.toolbar` modifier to NavigationSplitView in ContentView with:

1. **Leading placement** - Folder navigation group:
   - "Escolher Pasta" button (folder icon + text, or just folder.badge.plus icon)
   - Current directory path display (truncated, caption style)

2. Remove the inline header VStack with button and path from sidebar (lines 13-34)

3. Keep the Divider and List below - the toolbar replaces only the header controls

**Implementation notes:**
- Use `ToolbarItemGroup(placement: .navigation)` for leading items
- Path can use `.help()` modifier to show full path on hover
- Button should trigger existing `vm.chooseDirectory()` async call
- Toolbar items need access to vm - already available via @StateObject

**Style guidance:**
- Finder uses subtle toolbar with icon buttons
- Consider using `.symbolRenderingMode(.hierarchical)` for SF Symbols
  </action>
  <verify>xcodebuild -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset build 2>&1 | grep -E "(error:|BUILD SUCCEEDED)"</verify>
  <done>Toolbar shows folder picker and path display, inline header removed from sidebar</done>
</task>

<task type="auto">
  <name>Task 2: Move caption actions to toolbar</name>
  <files>lora-dataset/lora-dataset/ContentView.swift</files>
  <action>
Add caption action buttons to toolbar and remove from DetailView:

1. **Trailing placement** - Caption actions group:
   - "Recarregar" button (arrow.clockwise icon) - reload caption from disk
   - "Salvar" button (square.and.arrow.down icon, or checkmark) - save caption
   - Disable save when not dirty (use vm.selectedIsDirty)
   - Disable both when no selection (vm.selectedID == nil)

2. Remove from DetailView:
   - "Recarregar Caption" button (line 192-194)
   - "Salvar" button (lines 230-235, including HStack wrapper)
   - Keep the caption header (captionURL.lastPathComponent) - it provides context

3. **Implementation notes:**
   - Use `ToolbarItemGroup(placement: .primaryAction)` for trailing items
   - Save button uses existing `vm.saveSelected()` method
   - Reload button uses existing `vm.reloadCaptionForSelected()` method
   - Both buttons need `@ObservedObject var vm` - already available in ContentView
   - Consider adding `.help()` for button tooltips

**Visual consistency:**
- Match button style with folder picker (icon-only or icon+label depending on toolbar width)
- Use `.disabled()` modifier for conditional enabling
  </action>
  <verify>xcodebuild -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset build 2>&1 | grep -E "(error:|BUILD SUCCEEDED)"</verify>
  <done>Save and Reload buttons in toolbar, removed from DetailView, properly enabled/disabled based on selection and dirty state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Native macOS toolbar with folder navigation and caption action controls</what-built>
  <how-to-verify>
    1. Run: open lora-dataset/lora-dataset.xcodeproj and Cmd+R to build/run
    2. Check toolbar appearance:
       - Leading side has folder picker button
       - Path display visible (either in toolbar or as window subtitle)
       - Trailing side has Save and Reload buttons
    3. Test folder picker:
       - Click folder button, select a directory
       - Verify path updates in toolbar
    4. Test caption actions:
       - Select an image
       - Verify Save is disabled when caption unchanged
       - Edit caption text
       - Verify Save becomes enabled
       - Click Save, verify it disables again
       - Verify Reload reloads caption from disk
    5. Visual check:
       - Toolbar looks native/Finder-like
       - No leftover buttons in sidebar or detail view
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `xcodebuild -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset build` succeeds
- [ ] Toolbar visible with all controls
- [ ] Folder picker works from toolbar
- [ ] Save/Reload buttons work from toolbar
- [ ] No inline buttons remain in sidebar header or detail view
- [ ] Cmd+S still works (via existing menu command)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Toolbar has Finder-like appearance
- No controls duplicated (toolbar is single source)
- Existing keyboard shortcuts still work
</success_criteria>

<output>
After completion, create `.planning/phases/05-toolbar-integration/05-01-SUMMARY.md`:

# Phase 5 Plan 1: Toolbar Integration Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `lora-dataset/lora-dataset/ContentView.swift` - Added toolbar, removed inline controls

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for next milestone or feedback gathering.
</output>
