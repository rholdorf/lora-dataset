---
phase: 02-save-enhancements
plan: 01
type: execute
---

<objective>
Add Cmd+S keyboard shortcut, File → Save menu item, and visual dirty indicator.

Purpose: Provide standard macOS save workflow with visual feedback for unsaved changes.
Output: Working save shortcut, File menu integration, and dirty indicator dot in sidebar.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Codebase constraints:**
- Pure SwiftUI/AppKit - no external dependencies
- macOS 15.5+ deployment target
- Follow existing MVVM pattern with @MainActor ViewModel
- Use existing `vm.saveSelected()` method for actual save operation

**Prior phase context:**
@.planning/phases/01-directory-tree-navigation/01-02-SUMMARY.md

**Decisions from Phase 1 affecting this phase:**
- Local @State for List selection synced via Task - same pattern for FocusedValue
- Task-wrapped onChange handlers to defer state updates

**Relevant source files:**
@lora-dataset/lora-dataset/lora_datasetApp.swift
@lora-dataset/lora-dataset/ContentView.swift
@lora-dataset/lora-dataset/DatasetViewModel.swift
@lora-dataset/lora-dataset/ImageCaptionPair.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track dirty state in ImageCaptionPair and ViewModel</name>
  <files>lora-dataset/lora-dataset/ImageCaptionPair.swift, lora-dataset/lora-dataset/DatasetViewModel.swift</files>
  <action>
1. In ImageCaptionPair.swift:
   - Add `savedCaptionText: String` property to store the last-saved version
   - Initialize `savedCaptionText` to the loaded caption in the init
   - Add computed property `var isDirty: Bool { captionText != savedCaptionText }`

2. In DatasetViewModel.swift:
   - Update `scanCurrentDirectory()` to set both `captionText` and `savedCaptionText` to the loaded content
   - Update `saveSelected()` to set `savedCaptionText = captionText` after successful save
   - Add computed property `var selectedIsDirty: Bool` that returns the selected pair's isDirty status (or false if no selection)
  </action>
  <verify>xcodebuild -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset build succeeds</verify>
  <done>ImageCaptionPair has isDirty computed property, ViewModel tracks dirty state, saveSelected updates savedCaptionText</done>
</task>

<task type="auto">
  <name>Task 2: Add File menu with Save command and Cmd+S shortcut</name>
  <files>lora-dataset/lora-dataset/lora_datasetApp.swift, lora-dataset/lora-dataset/ContentView.swift</files>
  <action>
1. Create FocusedValueKey for ViewModel access:
   - Define `struct DatasetViewModelKey: FocusedValueKey { typealias Value = DatasetViewModel }`
   - Extend FocusedValues with `var datasetViewModel: DatasetViewModel?`

2. In lora_datasetApp.swift:
   - Add `.commands { ... }` modifier to WindowGroup
   - Add CommandGroup(replacing: .saveItem) with Button "Save" that calls focused ViewModel's saveSelected()
   - Apply `.keyboardShortcut("s", modifiers: .command)` to the Save button
   - Disable Save button when `focusedViewModel?.selectedIsDirty != true`

3. In ContentView.swift:
   - Add `.focusedValue(\.datasetViewModel, vm)` to the NavigationSplitView to expose ViewModel to menu commands
  </action>
  <verify>Build succeeds, File menu shows "Save" with Cmd+S shortcut visible</verify>
  <done>File → Save menu item exists with Cmd+S shortcut, disabled when no unsaved changes</done>
</task>

<task type="auto">
  <name>Task 3: Add dirty indicator to sidebar file list</name>
  <files>lora-dataset/lora-dataset/ContentView.swift</files>
  <action>
1. In the "Arquivos" section ForEach:
   - After the filename Text, add a small circle indicator when pair.isDirty is true
   - Use `Image(systemName: "circle.fill")` with `.font(.system(size: 6))` and `.foregroundColor(.orange)`
   - Position it with appropriate spacing (use HStack with spacing)

2. Alternative simpler approach - prepend bullet to filename:
   - Modify Text to show "• " + filename when isDirty
   - Use `.foregroundColor(.primary)` for normal, but make the bullet orange

Choose the SF Symbol approach for cleaner look.
  </action>
  <verify>Build succeeds, dirty indicator visible in UI when caption is edited</verify>
  <done>Orange dot appears next to filename in sidebar when caption has unsaved changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Save enhancements: Cmd+S shortcut, File menu, dirty indicator</what-built>
  <how-to-verify>
    1. Run: Open Xcode and run the app (Cmd+R)
    2. Select a folder with images
    3. Select an image from the sidebar
    4. Edit the caption text (add some characters)
    5. Verify: Orange dot appears next to the filename in sidebar
    6. Verify: File menu shows "Save" with "⌘S" shortcut
    7. Press Cmd+S
    8. Verify: Orange dot disappears (caption saved)
    9. Verify: File → Save menu item is now disabled (no unsaved changes)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `xcodebuild -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset build` succeeds
- [ ] No TypeScript/Swift errors or warnings
- [ ] Cmd+S saves the current caption
- [ ] File → Save menu item works
- [ ] Dirty indicator appears when caption is edited
- [ ] Dirty indicator disappears after saving
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Cmd+S keyboard shortcut saves the selected caption
- File → Save menu item visible and functional
- Visual dirty indicator shows unsaved changes
- Save button and menu item disabled when no unsaved changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-save-enhancements/02-01-SUMMARY.md`:

# Phase 02-01: Save Enhancements Summary

**[Substantive one-liner - what shipped]**

## Performance

- **Duration:** [X min]
- **Started:** [timestamp]
- **Completed:** [timestamp]
- **Tasks:** 4 (3 auto + 1 checkpoint)
- **Files modified:** [count]

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Deviations from Plan

[Any changes from original plan, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

[Ready for Phase 3: Security & Persistence, or blockers]

---
*Phase: 02-save-enhancements*
*Completed: [date]*
</output>
