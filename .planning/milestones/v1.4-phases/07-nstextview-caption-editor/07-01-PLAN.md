---
phase: 07-nstextview-caption-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lora-dataset/lora-dataset/CaptionEditorView.swift
  - lora-dataset/lora-dataset/ContentView.swift
  - lora-dataset/lora-datasetTests/CaptionEditorViewTests.swift
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]

must_haves:
  truths:
    - "Misspelled words in the caption editor show red underlines as the user types"
    - "Grammar issues show green underlines in the caption editor"
    - "User can right-click any word to see Look Up in the context menu"
    - "Smart quotes and smart dashes do not activate when typing quotation marks or double-hyphens"
    - "Spell checking language switches automatically when typing in a different language"
    - "Undo history is cleared when switching between images"
    - "Auto-correction, auto-capitalization, link detection, and text replacement are all disabled"
  artifacts:
    - path: "lora-dataset/lora-dataset/CaptionEditorView.swift"
      provides: "NSViewRepresentable wrapping NSTextView with LoRA-safe settings"
      exports: ["CaptionEditorView"]
    - path: "lora-dataset/lora-dataset/ContentView.swift"
      provides: "Updated detail pane using CaptionEditorView instead of TextEditor"
      contains: "CaptionEditorView"
    - path: "lora-dataset/lora-datasetTests/CaptionEditorViewTests.swift"
      provides: "Unit tests for NSTextView property configuration"
      contains: "CaptionEditorViewTests"
  key_links:
    - from: "CaptionEditorView.swift"
      to: "ContentView.swift"
      via: "CaptionEditorView(text: Binding) replacing TextEditor"
      pattern: "CaptionEditorView\\(text:"
    - from: "CaptionEditorView.Coordinator"
      to: "NSTextView"
      via: "NSTextViewDelegate textDidChange syncing text back to binding"
      pattern: "func textDidChange"
    - from: "CaptionEditorView.Coordinator"
      to: "NSUndoManager"
      via: "undoManager(for:) returning dedicated per-editor undo manager"
      pattern: "func undoManager\\(for"
---

<objective>
Create an NSTextView-based caption editor with spell check, grammar check, dictionary lookup, and LoRA-safe substitution settings, then integrate it into ContentView replacing the existing TextEditor.

Purpose: Enable native macOS text editing features (spell check, grammar, Look Up) while protecting LoRA training data from silent corruption by smart quotes, auto-correction, and other automatic substitutions.
Output: CaptionEditorView.swift (new), updated ContentView.swift, CaptionEditorViewTests.swift (new)
</objective>

<execution_context>
@/Users/rui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-nstextview-caption-editor/07-RESEARCH.md

@lora-dataset/lora-dataset/ContentView.swift
@lora-dataset/lora-dataset/ZoomablePannableImage.swift
@lora-dataset/lora-dataset/ImageCaptionPair.swift

<interfaces>
<!-- Existing binding pattern used at ContentView.swift:245-248 -->
From ContentView.swift (lines 245-248, the code to replace):
```swift
TextEditor(text: Binding(
    get: { vm.pairs[idx].captionText },
    set: { vm.pairs[idx].captionText = $0 }
))
.font(.body)
.overlay(
    RoundedRectangle(cornerRadius: 4)
        .stroke(Color.secondary.opacity(0.5))
)
.frame(minHeight: 200)
```

From ZoomablePannableImage.swift (established NSViewRepresentable pattern):
```swift
struct ZoomablePannableImage: NSViewRepresentable {
    // Uses Coordinator pattern with isUpdatingProgrammatically flag
    // makeNSView returns the NSView, updateNSView syncs state
    // Coordinator bridges between AppKit and SwiftUI bindings
}
```

From ImageCaptionPair.swift (dirty state):
```swift
struct ImageCaptionPair: Identifiable, Equatable {
    let id: UUID
    var captionText: String      // Current text (mutable, bound to editor)
    var savedCaptionText: String  // Last-saved text (dirty = captionText != savedCaptionText)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CaptionEditorView with NSTextView and unit tests</name>
  <files>lora-dataset/lora-dataset/CaptionEditorView.swift, lora-dataset/lora-datasetTests/CaptionEditorViewTests.swift</files>
  <behavior>
    - EDIT-01: After makeNSView, the NSTextView has isContinuousSpellCheckingEnabled == true
    - EDIT-02: After makeNSView, the NSTextView has isGrammarCheckingEnabled == true
    - EDIT-04: After makeNSView, isAutomaticQuoteSubstitutionEnabled == false AND isAutomaticDashSubstitutionEnabled == false
    - EDIT-05: NSSpellChecker.shared.automaticallyIdentifiesLanguages == true
    - LoRA-safe: isAutomaticSpellingCorrectionEnabled == false, isAutomaticTextReplacementEnabled == false, isAutomaticLinkDetectionEnabled == false, isAutomaticDataDetectionEnabled == false
    - isRichText == false
    - allowsUndo == true
    - Coordinator provides a dedicated NSUndoManager via undoManager(for:)
    - textDidChange syncs NSTextView.string back to the parent binding (guarded by isUpdatingProgrammatically)
  </behavior>
  <action>
    **Tests first** (CaptionEditorViewTests.swift):

    Create a test file that imports `@testable import lora_dataset` and `AppKit`. Use Swift Testing framework (`import Testing`, `@Test`, `#expect`) to match the existing test file pattern. Tests should:
    1. Instantiate `CaptionEditorView(text: .constant("test"))`
    2. Call `makeNSView(context:)` to get the NSScrollView
    3. Extract the NSTextView via `scrollView.documentView as! NSTextView`
    4. Assert all required properties

    Note: Creating an `NSViewRepresentable.Context` in tests requires using `NSHostingView` or a workaround. The simplest approach: create a helper method in CaptionEditorView that configures an NSTextView (called by both makeNSView and tests), or test by instantiating through `NSHostingView`. Alternatively, extract the configuration into a static `configure(_ textView: NSTextView)` method that tests can call directly on a plain NSTextView.

    **Implementation** (CaptionEditorView.swift):

    Create `CaptionEditorView: NSViewRepresentable` following the complete skeleton from 07-RESEARCH.md (lines 281-346). Key points:

    - `makeNSView` returns `NSScrollView` from `NSTextView.scrollableTextView()`
    - Set `scrollView.drawsBackground = false` to blend with SwiftUI
    - Configure ALL LoRA-safe properties: disable auto-correction, auto-capitalization (set `isAutomaticTextCompletionEnabled = false` if available), smart quotes, smart dashes, text replacement, link detection, data detection
    - Enable: continuous spell checking, grammar checking, allowsUndo
    - Set `isRichText = false`
    - Font: use `.monospacedSystemFont(ofSize: 13, weight: .regular)` — monospace is appropriate for training data captions
    - Set `textContainerInset = NSSize(width: 8, height: 8)` for internal padding
    - `updateNSView`: guard with `isUpdatingProgrammatically`, clear undo manager and set `tv.string = text` when text differs (handles image switch and reload)
    - Coordinator: `NSObject, NSTextViewDelegate` with `textDidChange` syncing back to binding, `undoManager(for:)` returning dedicated `textViewUndoManager`

    EDIT-05 (auto-language detection): Confirm `NSSpellChecker.shared.automaticallyIdentifiesLanguages` is true. If not, set it to true in `makeNSView`. This is typically already the system default.

    EDIT-03 (Look Up): No code needed — NSTextView includes "Look Up" in its default context menu. Will be verified manually.
  </action>
  <verify>
    <automated>cd /Users/rui/src/pg/lora-dataset/lora-dataset && xcodebuild test -project lora-dataset.xcodeproj -scheme lora-dataset -only-testing:lora-datasetTests/CaptionEditorViewTests 2>&1 | tail -30</automated>
  </verify>
  <done>CaptionEditorView.swift exists with all NSTextView properties configured. Unit tests pass asserting spell check enabled, grammar check enabled, smart substitutions disabled, auto-language detection active, and LoRA-safe settings applied.</done>
</task>

<task type="auto">
  <name>Task 2: Replace TextEditor with CaptionEditorView in ContentView</name>
  <files>lora-dataset/lora-dataset/ContentView.swift</files>
  <action>
    In ContentView.swift, replace lines 245-254 (the TextEditor block with its .font and .overlay modifiers) with:

    ```swift
    CaptionEditorView(text: Binding(
        get: { vm.pairs[idx].captionText },
        set: { vm.pairs[idx].captionText = $0 }
    ))
    .frame(minHeight: 200)
    ```

    The `.font(.body)` modifier is removed because font is configured inside CaptionEditorView's `makeNSView`. The `.overlay(RoundedRectangle...)` border is removed because NSScrollView provides its own border (or keep it if it looks good — use judgment). The `.frame(minHeight: 200)` stays to ensure minimum editor height.

    After replacement, build the project to confirm no compilation errors:
    ```
    xcodebuild build -project lora-dataset.xcodeproj -scheme lora-dataset
    ```
  </action>
  <verify>
    <automated>cd /Users/rui/src/pg/lora-dataset/lora-dataset && xcodebuild build -project lora-dataset.xcodeproj -scheme lora-dataset 2>&1 | tail -10</automated>
  </verify>
  <done>ContentView.swift uses CaptionEditorView instead of TextEditor. Project builds with zero errors. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify caption editor features in running app</name>
  <action>
    Human verifies the NSTextView-based caption editor in the running app. Steps:
    1. Open the app and select an image with a caption
    2. EDIT-01: Type a misspelled word (e.g., "teh") — verify red underline appears
    3. EDIT-02: Type a grammatically incorrect sentence (e.g., "He go store") — verify green underline appears
    4. EDIT-03: Right-click on any word — verify "Look Up" appears in the context menu; click it to confirm dictionary popup
    5. EDIT-04: Type a double-quote character (") — verify it stays as a straight quote, NOT a curly/smart quote. Type two hyphens (--) — verify they stay as two hyphens, NOT an em-dash
    6. EDIT-05: Type a sentence in a different language (e.g., Portuguese or Spanish) — verify spell check underlines adapt to that language
    7. Undo: Type some text, press Cmd+Z — verify undo works. Switch to a different image, press Cmd+Z — verify it does NOT undo text from the previous image
    8. Verify the editor looks reasonable (font, padding, scrolling behavior)
  </action>
  <verify>User confirms all 5 EDIT requirements work correctly in the running app</verify>
  <done>User types "approved" confirming all caption editor features work as expected</done>
</task>

</tasks>

<verification>
- `xcodebuild test -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset` — all tests pass
- `xcodebuild build -project lora-dataset/lora-dataset.xcodeproj -scheme lora-dataset` — builds clean
- Manual verification of all 5 EDIT requirements in the running app
</verification>

<success_criteria>
- CaptionEditorView.swift exists and wraps NSTextView with all LoRA-safe settings
- ContentView.swift uses CaptionEditorView instead of TextEditor
- Unit tests verify NSTextView property configuration (EDIT-01, EDIT-02, EDIT-04, EDIT-05)
- Manual verification confirms Look Up works (EDIT-03)
- Project builds and all tests pass
- Caption editing works end-to-end: type, save, reload, undo
</success_criteria>

<output>
After completion, create `.planning/phases/07-nstextview-caption-editor/07-01-SUMMARY.md`
</output>
